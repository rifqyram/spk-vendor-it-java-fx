# Database Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:spk_vendor_it_db}?TimeZone=UTC
spring.datasource.username=root
spring.datasource.password=password

# Auth Configuration
phi.super-admin.email=${SUPER_ADMIN_EMAIL}
phi.super-admin.password=${SUPER_ADMIN_PASSWORD}